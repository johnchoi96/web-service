spring:
  config:
    import:
      apikeys.yml, app-metadata.yml, admin-keys.yml

server:
  port: 8080

logging:
  level:
    root: INFO

scheduler:
  enabled: false

gcp:
  firebase:
    serviceAccountPath: dummy-service-account-path