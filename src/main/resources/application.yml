spring:
  config:
    import:
      optional:file:secrets/apikeys.yml, optional:file:secrets/admin-keys.yml, app-metadata.yml

server:
  port: 8080

gcp:
  firebase:
    serviceAccountPath: secrets/firebase-service-account.json